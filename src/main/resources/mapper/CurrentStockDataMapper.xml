<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mapper.CurrentStockDataMapper">

    <!-- 结果映射 -->
    <resultMap id="CurrentStockDataResultMap" type="org.example.entity.CurrentStockData">
        <id property="id" column="id"/>
        <result property="stockCode" column="stock_code"/>
        <result property="stockName" column="stock_name"/>
        <result property="tradeDate" column="trade_date"/>
        <result property="openPrice" column="open_price"/>
        <result property="highPrice" column="high_price"/>
        <result property="lowPrice" column="low_price"/>
        <result property="closePrice" column="close_price"/>
        <result property="volume" column="volume"/>
        <result property="turnover" column="turnover"/>
        <result property="floatMarketValue" column="float_market_value"/>
        <result property="totalMarketValue" column="total_market_value"/>
        <result property="industryLevel1" column="industry_level1"/>
        <result property="industryLevel2" column="industry_level2"/>
        <result property="industryLevel3" column="industry_level3"/>
        <result property="ma5" column="ma5"/>
        <result property="ma10" column="ma10"/>
        <result property="ma20" column="ma20"/>
        <result property="volumeRatio" column="volume_ratio"/>
        <result property="turnoverRate" column="turnover_rate"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <!-- 通用字段 -->
    <sql id="Base_Column_List">
        id, stock_code, stock_name, trade_date, open_price, high_price, low_price, close_price,
        volume, turnover, float_market_value, total_market_value,
        industry_level1, industry_level2, industry_level3, ma5, ma10, ma20, volume_ratio, turnover_rate, created_at, updated_at
    </sql>

    <!-- 插入股票数据 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO current_stock_data (
            stock_code, stock_name, trade_date, open_price, high_price, low_price, close_price,
            volume, turnover, float_market_value, total_market_value,
            industry_level1, industry_level2, industry_level3
        ) VALUES (
            #{stockCode}, #{stockName}, #{tradeDate}, #{openPrice}, #{highPrice}, #{lowPrice}, #{closePrice},
            #{volume}, #{turnover}, #{floatMarketValue}, #{totalMarketValue},
            #{industryLevel1}, #{industryLevel2}, #{industryLevel3}
        )
    </insert>

    <!-- 批量插入 -->
    <insert id="insertBatch" useGeneratedKeys="true" keyProperty="id">
        INSERT IGNORE INTO current_stock_data (
            stock_code, stock_name, trade_date, open_price, high_price, low_price, close_price,
            volume, turnover, float_market_value, total_market_value,
            industry_level1, industry_level2, industry_level3
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.stockCode}, #{item.stockName}, #{item.tradeDate}, #{item.openPrice},
                #{item.highPrice}, #{item.lowPrice}, #{item.closePrice}, #{item.volume}, #{item.turnover},
                #{item.floatMarketValue}, #{item.totalMarketValue}, #{item.industryLevel1},
                #{item.industryLevel2}, #{item.industryLevel3}
            )
        </foreach>
    </insert>

    <!-- 检查记录是否存在 -->
    <select id="exists" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM current_stock_data
        WHERE stock_code = #{stockCode}
        AND trade_date = #{tradeDate}
    </select>

    <!-- 查询所有记录 -->
    <select id="selectAll" resultMap="CurrentStockDataResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM current_stock_data
        ORDER BY trade_date DESC
    </select>

    <!-- 根据股票代码查询 -->
    <select id="selectByStockCode" resultMap="CurrentStockDataResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM current_stock_data
        WHERE stock_code = #{stockCode}
        ORDER BY trade_date DESC
    </select>

    <!-- 根据日期范围查询 -->
    <select id="selectByDateRange" resultMap="CurrentStockDataResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM current_stock_data
        WHERE trade_date BETWEEN #{startDate} AND #{endDate}
        ORDER BY trade_date DESC
    </select>

    <!-- 删除超过1年的旧数据 -->
    <delete id="deleteOldData">
        DELETE FROM current_stock_data
        WHERE trade_date &lt; #{beforeDate}
    </delete>

    <!-- 查询指定日期范围内指定股票的交易数据（按日期升序排序） -->
    <select id="selectByStockCodeAndDateRange" resultMap="CurrentStockDataResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM current_stock_data
        WHERE stock_code = #{stockCode}
        AND trade_date BETWEEN #{startDate} AND #{endDate}
        ORDER BY trade_date ASC
    </select>

    <!-- 查询所有股票代码 -->
    <select id="selectAllStockCodes" resultType="string">
        SELECT DISTINCT stock_code
        FROM current_stock_data
        ORDER BY stock_code
    </select>

    <!-- 更新技术指标 -->
    <update id="updateTechnicalIndicators" parameterType="org.example.entity.CurrentStockData">
        UPDATE current_stock_data
        SET ma5 = #{ma5},
            ma10 = #{ma10},
            ma20 = #{ma20},
            volume_ratio = #{volumeRatio},
            turnover_rate = #{turnoverRate}
        WHERE id = #{id}
    </update>

    <!-- 根据股票代码查询股票名称 -->
    <select id="findStockNameByCode" resultType="string">
        SELECT stock_name
        FROM current_stock_data
        WHERE stock_code = #{stockCode}
        AND stock_name IS NOT NULL
        AND stock_name != ''
        LIMIT 1
    </select>

    <!-- 查询所有不同的股票代码和名称 -->
    <select id="findDistinctStockCodeAndName" resultType="map">
        SELECT DISTINCT stock_code as stockCode, stock_name as stockName
        FROM current_stock_data
        WHERE stock_name IS NOT NULL
        AND stock_name != ''
        ORDER BY stock_code
    </select>

    <!-- 查询所有不同的股票代码、名称和行业信息 -->
    <select id="findDistinctStockInfo" resultType="map">
        SELECT DISTINCT
            stock_code as stockCode,
            stock_name as stockName,
            industry_level1 as industryLevel1,
            industry_level2 as industryLevel2,
            industry_level3 as industryLevel3
        FROM current_stock_data
        WHERE stock_name IS NOT NULL
        AND stock_name != ''
        ORDER BY stock_code
    </select>

    <!-- 查询指定股票中未计算技术指标的记录 -->
    <select id="selectUncalculatedByStockCode" resultMap="CurrentStockDataResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM current_stock_data
        WHERE stock_code = #{stockCode}
        AND (ma5 IS NULL OR ma10 IS NULL OR ma20 IS NULL OR volume_ratio IS NULL)
        ORDER BY trade_date ASC
    </select>

    <!-- 查询某个股票最后一条已计算指标的记录 -->
    <select id="selectLastCalculatedRecord" resultMap="CurrentStockDataResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM current_stock_data
        WHERE stock_code = #{stockCode}
        AND ma5 IS NOT NULL
        AND ma10 IS NOT NULL
        AND ma20 IS NOT NULL
        ORDER BY trade_date DESC
        LIMIT 1
    </select>

    <!-- 查询某个股票在指定日期之后未计算指标的记录 -->
    <select id="selectUncalculatedAfterDate" resultMap="CurrentStockDataResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM current_stock_data
        WHERE stock_code = #{stockCode}
        AND trade_date > #{afterDate}
        AND (ma5 IS NULL OR ma10 IS NULL OR ma20 IS NULL)
        ORDER BY trade_date ASC
    </select>

    <!-- 查询指定日期的所有股票代码和名称 -->
    <select id="findStockNamesByDate" resultType="map">
        SELECT DISTINCT
            stock_code as stockCode,
            stock_name as stockName
        FROM current_stock_data
        WHERE trade_date = #{tradeDate}
        AND stock_name IS NOT NULL
        AND stock_name != ''
        ORDER BY stock_code
    </select>

    <!-- 根据股票代码更新所有该股票的名称 -->
    <update id="updateStockNameByCode">
        UPDATE current_stock_data
        SET stock_name = #{stockName},
            updated_at = CURRENT_TIMESTAMP
        WHERE stock_code = #{stockCode}
    </update>

    <!-- 查询指定股票在指定日期的数据 -->
    <select id="selectByStockCodeAndDate" resultMap="CurrentStockDataResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM current_stock_data
        WHERE stock_code = #{stockCode}
        AND trade_date = #{tradeDate}
        LIMIT 1
    </select>

    <!-- 查询指定股票在指定日期之后的第N个交易日的数据 -->
    <select id="selectByStockCodeAndDateOffset" resultMap="CurrentStockDataResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM current_stock_data
        WHERE stock_code = #{stockCode}
        AND trade_date > #{afterDate}
        ORDER BY trade_date ASC
        LIMIT #{offset}, 1
    </select>

</mapper>
